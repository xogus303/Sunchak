# 이 파일은 "필요한 키 목록·형식" 참고용이다. 실제 값은 기기별 .env(gitignore)에 채운다.

# 백엔드(apps/api) 주소 — 브라우저에서 직접 fetch/EventSource로 호출하므로
# NEXT_PUBLIC_ 접두어 필요(Next.js가 이 접두어만 클라이언트 번들에 포함시킨다).
NEXT_PUBLIC_API_URL="http://localhost:3001"
